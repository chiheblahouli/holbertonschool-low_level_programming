#include <stdlib.h>
/**
 * malloc_checked - func allocates memory using malloc
 *@b: input
 *Return: void pointer
 */
void *malloc_checked(unsigned int b)
{
char *p;

		m = malloc(b);
		if (p == NULL)

		exit(98);

		else

		return (p);
}
