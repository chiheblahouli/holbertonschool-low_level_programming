#include "holberton.h"
/**
* _strcat - concatenates two strings.
* @src : source
* @dest : destination
*
*return : dest
*/
char *_strcat(char *dest, char *src)
{


for (l = 0; dest[l] != '\0'; l++)
{
}
for (i = 0; src[i] != '\0'; i++)
{
dest [l+i] = '\0';
}
return (dest);
}
